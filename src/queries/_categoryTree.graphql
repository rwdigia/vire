fragment categoryTree on CategoryTree {
  id: uid
  uid
  url_key
  url_path
  name
  breadcrumbs {
    id: category_id
    category_url_path
    category_name
  }
  children {
    id: uid
    uid
    url_key
    url_path
    name
  }
}
